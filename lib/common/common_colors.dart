import 'package:flutter/material.dart';

class AppColors {
  // Primary & Button Colors
  static const Color primary = Color(0xFF2196F3);
  static const Color primaryColor = Color(0xFF2196F3);
  static const Color buttonColor = Color(0xFF2196F3);
  static const Color primaryDark = Color(0xFF1976D2);
  static const Color primaryLight = Color(0xFFBBDEFB);
  static const Color accent = Color(0xFF03DAC6);

  // News Brand Colors
  static const Color newsRed = Color(0xFFFE2623);
  static const Color red = Color(0xFFFE2623);

  // Neutral Colors
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color transparent = Colors.transparent;

  // Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textLight = Color(0xFF9E9E9E);

  // Background & Surface Colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;
  static const Color greyLight = Color(0xFFFAFAFA);
  static const Color border = Color(0xFFE0E0E0);

  // Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFB300);
  static const Color info = Color(0xFF2196F3);
}

// Aliases for convenience
typedef AppColor = AppColors;
typedef AppColours = AppColors;
typedef AppColour = AppColors;
typedef CommonColors = AppColors;
